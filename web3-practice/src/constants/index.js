const tokenAbi = require("./tokenAbi.json");
const tokenAddress = require("./tokenAddress.json");
const marketplaceAbi = require("./marketplaceAbi.json");
const marketplaceAddress = require("./marketplaceAddress.json");

module.exports = {
  tokenAbi,
  tokenAddress,
  marketplaceAbi,
  marketplaceAddress,
};
